pip install Django
---would install the Django package

pip list 
---would list all the package that is installed.

pip install Django
---says that the package is already installed.

pip freeze
---display the version differently.

pip freeze > requirments.txt
---It would print out the packages and their version information into the txt file.

pip uninstall---uninstall

pip show---show the details

pip search Flask---search packages whose name or summary contains Flask.